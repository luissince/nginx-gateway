server {
    access_log /var/log/nginx/api_access.log main; # Each API may also log to a separate file

    #listen 
    listen       80;
    listen  [::]:80;

    server_name loschasquis.pe;

    # API definitions, one per file

    location / {
        return 200 "HOLA CHASQUIS";
    }
}